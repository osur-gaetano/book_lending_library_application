<div>
  <h1 class="font-bold text-4xl">Books in our Catalogue</h1>
  <div id="books">
    <% @books.each do |book| %>
      <div>
        <%= link_to book.title, book%>
      </div>
    <%end%>
</div>
